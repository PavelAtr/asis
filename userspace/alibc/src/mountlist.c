#include <mountlist.h>

/* NOT REALIZED */

struct mount_entry ret;

struct mount_entry *read_file_system_list (unsigned char need_fs_type)
{
    return &ret;
}

void free_mount_entry (struct mount_entry *entry)
{
}

